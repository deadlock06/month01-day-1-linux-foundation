# month01-day01-linux-foundation

## Objective
Deliver a tested backup script and basic Linux permissions demo.

## Quickstart
1. Make sure you are in the repo root
2. Run `make up`
3. Run `make test`

## What to ship
- `scripts/backup.sh`
- `tests/validate_backup.sh`
- `tests/validate_permissions.sh`
- `Makefile`
